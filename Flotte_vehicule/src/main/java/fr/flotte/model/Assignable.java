package fr.flotte.model;

// Une interface ne contient que des signatures de méthodes (pas de code)
public interface Assignable {
    boolean estDisponible(); // Oblige la classe à dire si elle est libre
}